# هيكل مشروع سين وجيم

## الملفات الحالية

- `8.html`: نقطة تشغيل الواجهة الحالية ومنطق اللعبة المؤقت.
- `css/theme.css`: ألوان الثيم والمتغيرات المشتركة.
- `css/base.css`: الأنماط الأساسية والجسم والشريط العام.
- `css/home.css`: الصفحة الرئيسية والإعدادات وتجهيز الفرق.
- `css/tournament.css`: مخطط البطولة والمواجهات والنتائج.
- `css/board.css`: لوحة الأسئلة وبطاقات النقاط.
- `css/question.css`: شاشة السؤال والمؤقت ووسائل المساعدة.
- `js/ui.js`: إدارة إظهار الصفحات وشارة الدور.
- `js/board.js`: توليد بطاقات لوحة الأسئلة وتصفيرها.
- `js/tournament.js`: بناء مخطط البطولة وترميز أسماء الفرق.
- `js/question-tools.js`: التلميح ومضاعفة النقاط وإعادة ضبط صندوق التلميح.
- `js/question.js`: مؤقت شاشة السؤال وتشغيله وإيقافه وإعادة ضبطه.
- `js/scoring.js`: احتساب النقاط وتحديث عدادات الفرق.
- `js/results.js`: عرض نتائج المواجهات والجولات والبطل.
- `js/history.js`: حفظ وعرض ومسح سجل المباريات.
- `js/question-bank.js`: إدارة بنك الأسئلة ومنع التكرار والبحث.
- `js/storage.js`: حفظ وقراءة ومسح سجل المباريات.
- `data/question-bank.js`: الأسئلة الأساسية للفئات الحالية.
- `data/games-questions.js`: أسئلة فئة الألعاب.
- `data/README.md`: طريقة إضافة الأسئلة.
- `views/README.md`: خطة قوالب HTML عند تشغيل المشروع عبر خادم محلي.
- `pages/`: صفحات دخول مستقلة لكل جزء من التطبيق، مرتبطة بالتطبيق الحالي عبر hash.

## قاعدة التنظيم

أضف الأسئلة داخل ملفات `data/` فقط. أضف الخدمات والمنطق المشترك داخل `js/`، وأضف تنسيقات المكونات داخل `css/`. لا تضع بيانات كبيرة داخل `8.html`.

## ملاحظة الصفحات

صفحات `pages/` تعمل على GitHub Pages كروابط مستقلة، بينما حالة اللعبة تبقى داخل `8.html` حتى لا تضيع عند التنقل. يمكن لاحقاً تحويلها إلى صفحات مستقلة بالكامل مع تخزين الحالة في `localStorage`.
